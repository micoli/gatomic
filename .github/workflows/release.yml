name: Release

on:
  push:
    tags:
      - "v*"
  release:
    types: [published]

permissions:
  contents: write

jobs:
  # Creates the GitHub release first, with a changelog auto-generated from
  # every commit/PR since the previous tag — GitHub's native release-notes
  # generator, no extra dependency. Build jobs then only attach binaries to
  # it, so the notes are written exactly once regardless of how many
  # targets build in parallel. Skipped when triggered by manually
  # publishing a release from the UI: it already exists with whatever
  # notes were written there, and re-generating would overwrite them.
  create-release:
    runs-on: ubuntu-latest
    steps:
      - name: Create release with auto-generated changelog
        if: github.event_name == 'push'
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

  build:
    name: ${{ matrix.target }}
    needs: create-release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            cross: false
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            cross: true
          - target: x86_64-apple-darwin
            os: macos-latest
            cross: false
          - target: aarch64-apple-darwin
            os: macos-latest
            cross: false
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            cross: false
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Install cross
        if: matrix.cross
        run: cargo install cross --locked

      - name: Build
        shell: bash
        run: |
          if [ "${{ matrix.cross }}" = "true" ]; then
            cross build --release --locked --target ${{ matrix.target }}
          else
            cargo build --release --locked --target ${{ matrix.target }}
          fi

      - name: Package
        id: package
        shell: bash
        run: |
          version="${GITHUB_REF_NAME}"
          bin="gatomic"
          dir="gatomic-${version}-${{ matrix.target }}"
          mkdir "$dir"
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            cp "target/${{ matrix.target }}/release/${bin}.exe" "$dir/"
            cp README.md "$dir/"
            archive="${dir}.zip"
            7z a "$archive" "$dir"
          else
            cp "target/${{ matrix.target }}/release/${bin}" "$dir/"
            cp README.md "$dir/"
            archive="${dir}.tar.gz"
            tar czf "$archive" "$dir"
          fi
          echo "archive=$archive" >> "$GITHUB_OUTPUT"

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.package.outputs.archive }}
          fail_on_unmatched_files: true
